<div class="user_profile_container">
  <script id="user_photo_show_template" type="text/x-handlebars-template">
  {{photos.message}}
  <p>
  {{photos.length}}
  </p>
  <p>
  {{photos.user_stash_count}}
  </p>
  <p>
  {{#each photos}}
    <div><span data-id="{{id}}"><img src={{image_url}}></span></div>
  {{/each}}
  <p>
  </script>
</div>

