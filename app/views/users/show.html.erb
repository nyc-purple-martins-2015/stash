<div class="container">
  <% if @error %>
      <p><%= @error %></p>
  <% else %>
    <span class="user_info">
      <%= @user_photos.length %> posts
    </span>

    <% @user_photos.each do |photo|%>
      <div id="polaroid">
        <figure class="front">
          <img src="<%= photo.image_url %>" width="200" height="200" alt="user_photo" />
          <figcaption><%= photo.dish_name %></figcaption>
        </figure>
        <figure class="back">
          <figcaption><%= photo.dish_name %> from</figcaption>
          <figcaption><%= photo.restaurant.name %> from</figcaption>
        </figure>
      </div>
    <% end %>
  <% end %>

<div><%= link_to "Sign out", signout_path, id: "sign_out" %></div>

</div>
