<div class="container">
<%= form_for photo, html: {multipart: true } do |f| %>

  <div class="top_form">
    <div class="photo_upload">
      <span class="btn btn-success fileinput-button" class: "app-button">
      <span>Upload a Photo</span>
      <%= f.file_field :image, accept: "image/*" %>
    </div>

    <div class="photo_preview_container">
    <div id="photo_preview" /> </div>
    </div> <br>

    <p><%= f.text_field :dish_name, value: nil, :placeholder => "Dish Name" %></p>

    <p><%= f.text_field :foodtags, value: nil, :placeholder => "Foodtags" %></p>

    <!-- You might want to think about storing your price ranges in a price_ranges 
    tablele with a foreign key in photo. The way these are it would be diffcult 
    for you to (for example)  show only places where cost <$20.  BUT don't you 
    already have a pricetags table? Could you use that here instead of 
    hard-coding ranges into the select?
    -->

    <p><%= f.select :pricetag, [['$0..$10', "$" ], ['$10..$15', "$$" ], ['$15..$20', "$$$" ],['$20+', "$$$$" ]] %></p>
  </div>


    <div class="bottom_form">
      <%= f.hidden_field :restaurant_name, id: "restaurant_name" %>
      <p><input id="searchTextField" id="pac-input" class="controls" type="text" placeholder="search for restaurant here."></p>

       <div id="restaurant_test"></div>

       <!-- why are you assigning ids to these fields instead of letting rails do it for you? -->

       <%= f.hidden_field :restaurant_rating, id: "restaurant_rating" %>
       <%= f.hidden_field :restaurant_address, id: "restaurant_address" %>
       <%= f.hidden_field :restaurant_phone, id: "restaurant_phone" %>
       <%= f.hidden_field :restaurant_website, id: "restaurant_website" %>

      <%= f.hidden_field :lat, id:"lat_field" %>
      <%= f.hidden_field :lng, id:"lng_field" %>


    <div class="clear_fix">
    </div>
    <!-- </div> -->

  <div class="map_container">
      <div id="map"></div>
  </div>

  <br>


    <p><%= submit_tag 'Share your Dish', id:"add_photo_button", class: "app-button" %></br>
  </div>
<% end %>
</div>
